# Configuración de Producción (Renombrar a .env en el servidor)

# ¡IMPORTANTE! Generar una key segura y única
# Puedes generarla con: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=cambiar_por_una_clave_segura_y_larga

# Debug desactivado en producción por seguridad
DEBUG=False

# IP del VPS y Dominio (separados por coma, sin espacios)
ALLOWED_HOSTS=72.62.80.238,inprocaf.com,www.inprocaf.com

# Orígenes de confianza para CSRF (necesario para HTTPS/Dominio)
CSRF_TRUSTED_ORIGINS=https://inprocaf.com,https://www.inprocaf.com

# Base de Datos (PostgreSQL)
# Formato: postgres://USUARIO:CONTRASEÑA@HOST:PUERTO/NOMBRE_DB
DATABASE_URL=postgres://inprocaf:1234@localhost:5432/inprocaf_db
